apiVersion: apps/v1
kind: Deployment
metadata:
  name: gfg-web
  namespace: gfg-v2
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gfg-web
  template:
    metadata:
      labels:
        app: gfg-web
    spec:
      containers:
      - name: gfg-web
        image: gfg-web-nginx:latest
        ports:
        - containerPort: 80
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: gfg-web
  namespace: gfg-v2
spec:
  type: ClusterIP
  selector:
    app: gfg-web
  ports:
  - port: 80
    targetPort: 80